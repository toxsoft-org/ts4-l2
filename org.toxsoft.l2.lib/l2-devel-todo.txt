Создание L2 версии4 (сейчас, 2026-03-14 в платформе версии 4 подсистема L2 взята из версии 3)
  (Примечание: тремя тирt '---' обозначется элемент TODO и степень выполнения вплоть до '+++' (сделано) 
  

Этап 1 - рефакторинг ru.toxsoft.l2.core
  +++ разделение кода на два проекта:
      org.toxsoft.l2.main - фактически только класс L2CoreMain с методом main() 
      org.toxsoft.l2.lib - библиотка, содержит все, кроме L2CoreMain
  +++ минимальный рефакторинг (убрать неиспользуемый код, разиб на множество, сократить размеры файлов исходников)
  --- сотзать тестовое окружение и проверить, l2.main.old_refactor.L2CoreMain работает как не отрефакторенная версия
  --- провести дополнительный рефакторинг (с постянным тестированием в созданном окружении):
      - убрать мусорный код (например, 
      

Этап 2 - выделение отдельного класса L2Application, нужно для того, чтобы приложение L2 можно было запускать как через
         L2CoreMain.main(), так и внутри контейнера скатлетов. Для этого, класс L2Application должен наследоваться
         от AbstractWubUnit.
  +-- создание класса и интерфейса (I)L2Application
  --- изменение в аргументах init(): например, ссылка SkConnection передается снаружи
  --- перенос кода из main() в L2Application и доработка самого L2CoreMain.main()
  --- переделка L2Application так, чтобы использовать doJob() (например, убрать все sleep())
  
  
Этап 3 - ввести  в l2.lib понятие ITag (из Modbus и OPC UA)
  --- создать интерфейс ITag и возможно, отдельный ITagDevice (???)
  --- сделать драйвер типа TagDevice, поставщик тегов из следубщих источников:
      - MODBUS
      - OPC UA
      - ???
  --- сделать SkIDE конфигураоры, соответствующие перечисленным драйверам (MODBUS, OPC, ...)
  --- добавить в HAL теги, сделать, что теги могут поставлять множество TagDevice
  --- сделать стандартный DLM работы с тегами (по аналогии, как сечас с Modbus/OPC)
  --- сделать SkIDE конфигуратор сопряжения тегов с USkat (конфигуратор один конфигруатор для всех тегов, но это не 
      отменяет специальных импортеров типа как существующий "Создатель описания системы из OPC UA")
  
  
Этап 4 - обеспечить работу ВНЕ L2CoreMain.main(), в рамках
  --- выделить в отдельный проект и сделать опциональным работу с резервированием
  --- убрать зависимости от skf-* репозитеориев из org.toxsoft.l2.lib:
      - опциональная работа с DataQuality
      - опциональная работа с RRI
  --- изменить логику работвы с В/В: вместо sleep() отдельный поток В/В и флаги для доступа к ним из doJob()
  --- изменить логику работвы с сетью: создать отдельный поток работы с SkConnection
  

